services:
  nyan-go:
    image: golang:1.27-alpine
    working_dir: /src
    command: go run ./cmd/nyan-go
    restart: unless-stopped
    init: true
    env_file: .env
    environment:
      SQLITE_PATH: /data/nyan-go.db
    volumes:
      - .:/src
      - nyan-go-dev-data:/data
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build

volumes:
  nyan-go-dev-data:
  go-mod-cache:
  go-build-cache:
